{
  "name": "socketio-rotation",
  "version": "0.0.1",
  "description": "Simple Socket.IO server to handle client rotation events",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.15.4",
    "socket.io": "^2.0.3"
  }
}
